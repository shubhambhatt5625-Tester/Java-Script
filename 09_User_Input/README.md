# Chapter 09 - User Input

This chapter covers taking user input in JavaScript (upcoming content).

## Key Takeaways

- User input can be captured via browser prompts, forms, and event listeners.
- `prompt()` returns user input as a string.
- Input should be validated and sanitized before use.
