let user_name = "S";
let user_password = "Q";
let isaccountlocked = true;

if ((user_name === "S" && user_password === "Q") && isaccountlocked) {
    console.log("Valid User");
}

else {
    console.log("You are Not a Valid User");
}