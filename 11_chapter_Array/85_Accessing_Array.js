let r = ["pass", "fail", "skip"];
console.log(r[1]);
console.log(r[0]);
console.log(r[2]);
// what it we want to access the last one without knowing the length, so here we will us .at
// (-3,-2, -1)
console.log(r.at(-1));
console.log(r.at(-3));
console.log(r.at(-2));
